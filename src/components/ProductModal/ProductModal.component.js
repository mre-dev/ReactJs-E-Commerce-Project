import PropTypes from 'prop-types';
import React from 'react';
import Styles from "assets/styles/components/ProductModal/ProductModal.module.css";

export const ProductModal = (props) => {
    return (
        <></>
    );
};

ProductModal.propTypes = {
    
};