import PropTypes from 'prop-types';
import React from 'react';
import Styles from "assets/styles/components/OrderModal/OrderModal.module.css";

export const OrderModal = (props) => {
    return (
        <></>
    );
};

OrderModal.propTypes = {
    
};